1. 创建一个工具站,拟命名为toolhub,用于提供在线工具服务,前端使用vue3,后端使用nodejs
2. 技术栈参考vue3 + vite + naiveui + highlightjs + pinia + axios + js-beautify + prettier 请按需要引入需要的库
3. 网站风格参考 json.cn。 
4. 网站功能：
   1. header 包含logo（toolhub）,菜单导航栏，搜索框，编排页面按钮，主题（暗色与明色）
   2. 菜单导航栏包含多个主要分类 格式化工具，加密工具，转换工具，图片工具，文本工具，其他工具
   3. 每个分类下包含多个工具，详情如下
      1. 格式化工具（一级菜单）
         数据格式（菜单分割线）
            Json格式化（二级菜单）组件包含一个输入框（提示输入json数据），输入框下包含按钮，格式化、压缩、复制、转义、去除转义。点击格式化后将格式化后的JSON放入输入框展示，输入框边缘可以点击+-来展开/收起JSON结构，如果数据有问题，就在输入框下面提示。点击压缩将输入框内的JSON压缩成一行展示，点击复制将输入框内的JSON复制到剪切板中，提示复制成功。点击转义将输入框内的JSON进行转义，点击去除转义将JSON内的转义去除。
            Xml格式化（二级菜单）组件包含一个输入框（提示输入xml数据），输入框下方包含按钮 格式化、压缩、复制 点击格式化后将格式化后的XML放入输入框展示，可以点击边缘的+-来展开收起，如果数据有问题，就在输入框下面提示。点击压缩将内容压缩成一行放入输入框内，点击复制将内容复制到剪切板中，提示复制成功
         编程语言
            Js格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Html格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Css格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Python格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Java格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            CSharp格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Go格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            PHP格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Ruby格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Kotlin格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Rust格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
         其他语言
            Shell/Bash格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Sql格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Markdown格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
            Dart格式化 包含输入框，输入框按钮包含格式化，复制按钮。点击格式化进行格式化，点击复制将内容复制到剪切板，提示复制成功
      注意每个工具都是一个独立的组件，你可以创建更基础的组件实现复用，编程语言等格式化可以使用prettier实现，js/html/css格式化可以使用js-beautify实现
      分类下的功能用一个view页面
      2. 加密工具(以下工具请根据加密算法特点进行页面设计实现)
         对称加密
            AES
            ChaCha20
            DES/3DES
         非对称加密
            RSA
            ECC
            Ed25519
         哈希算法
            SHA-1
            SHA-256
            SHA-512
            Bcrypt
            Argon2
         密钥交换
            Diffie-Hellman
            ECDH
            X25519
         数字签名
            RSA
            ECDSA
            Ed25519
         编码与解码
            Base64
            Hex
            URL编码
            HTML编码（如 < 转 &lt;）
            JWT编码解码
      注意每个工具都是一个独立的组件，你可以创建更基础的组件实现复用
      分类下的功能用一个view页面
      3. 转换工具
         时间转换
            时间戳转换 页面包含当前时间戳（随时间增长而增加），时间戳转日期（时间戳输入框 + 秒/毫秒选择框+转换按钮+时区选择 +转换日期结果），日期转时间戳（日期选择 + 时区 + 转换按钮 + 转换结果 + 秒/毫秒选择框）
            日期间计算（天数差，小时差）包含两个日期选择器 + 年/月/日/天/秒/毫秒选择 + 计算按钮 + 输入结果
            日期加减 包含时间选择 + 年/月/日/天/秒/毫秒选择 + 加/减选择 + 计算按钮 + 输出结果
         进制转换
            2进制/10进制/8进制/16进制 包含输入框 + 进制选择 + 计算按钮 + 输出框
         字符转换
            ASCII码 与 Unicode码转换（如 A → 65 → U+0041）
         数值转换
            科学计数法 到普通数值
            千分位分隔（1234567 → 1,234,567）
         单位转换
            存储单位转换：字节，KB，MB ，GB ，TB，PB。
            时间单位转换：毫秒， 秒， 分钟， 小时， 天。
            长度/温度/重量转换：米与英尺，摄氏度与华氏度、千克与磅。
            颜色格式转换：HEX，RGB。
            颜色选择器：提供调色板生成和颜色值提取。
         正则表达式工具
            正则测试器正则生成器
         Markdown 转 HTML：实时预览转换结果。
         二维码生成器：文本/URL 生成二维码图片。
         HTTP 状态码解释：输入状态码（如 404、503）显示含义和场景。
         User-Agent 解析：解析 User-Agent 字符串，显示设备、浏览器、操作系统信息。
         IP 地址查询：查询 IP 的地理位置和 ISP 信息。
      注意每个工具都是一个独立的组件，你可以创建更基础的组件实现复用
      分类下的功能用一个view页面
      4. 图片工具
            图片压缩
            图片转换
            图片旋转
            图片裁剪
            图片水印
            图片去水印
      注意每个工具都是一个独立的组件，你可以创建更基础的组件实现复用
      分类下的功能用一个view页面
      5. 文本工具
            大小写转换：全大写，全小写，首字母大写。
            字符串反转：反转字符顺序（如 hello -> olleh）。
            空格处理：去除首尾空格、压缩连续空格。
            文本替换：换行回车替换英文逗号，英文逗号替换换行回车
      注意每个工具都是一个独立的组件，你可以创建更基础的组件实现复用
      分类下的功能用一个view页面
      6. 其他工具
            二维码生成，二维码解码
            短网址生成，短网址解码，
            ip查询，ip定位，ip反查，ip归属地，ip归属地查询，ip归属地反查
            网页计算器
      注意每个工具都是一个独立的组件，你可以创建更基础的组件实现复用
      分类下的功能用一个view页面
   4. 搜索框 可以搜索已知工具，点击后跳转到目标页面
   5. 编排按钮 点击后跳转到单独页面，可以点击页面的+号添加工具
   6. 主题（暗色与明色）
   7. 底部包含：备案信息，友情链接，版权信息,联系我们

要求样式风格统一且界面友好，<重要>尽可能使用前端组件/能力实现<重要> 如果需要后端接口支持，请补充URL及数据结构到代码中（数据结构可以使用注释写），网站提供记忆功能，用户打开页面优先展示上一次填写的数据（cookie实现）

全栈开发即可，注意使用i18n实现国际化(中文+英文)

要求组件实现到components文件夹下（内部也要根据功能做分类），路由实现到router文件夹下，页面实现到views文件夹下（页面分大类即可）。

可以使用Vite创建项目，创建项目后所有的代码都在toolhub文件夹下
