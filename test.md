# A long title that will definitely exceed a small print width and show wrapping if applicable

This is a paragraph with many words to see how prose wrapping behaves. Prettier is an opinionated code formatter. It enforces a consistent style by parsing your code and re-printing it with its own rules that take the maximum line length into account, wrapping code when necessary. This is a very long line that should wrap.

- List item 1
- List item 2
  - Nested list item
A very long line inside a list item that should also be subject to print width and prose wrapping to see its behavior.

[A link](https://example.com) and some more text after the link to see how it all fits together.
